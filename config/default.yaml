to_cuda: cuda

raise_exception_if_gpus_not_available: true
neptune_project: null
use_neptune: false

paths:
  data_dir: ${hydra:runtime.cwd}/data
  results_dir: ${hydra:runtime.cwd}/results

defaults:
  - _self_
  - preprocessing: default
  - model: baseline
